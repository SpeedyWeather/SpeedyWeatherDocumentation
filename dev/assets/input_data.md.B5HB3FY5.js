import{_ as o,C as n,o as d,c as l,ao as s,j as a,a as r,E as i,w as h}from"./chunks/framework.DePoFsTh.js";const b=JSON.parse('{"title":"Input data","description":"","frontmatter":{},"headers":[],"relativePath":"input_data.md","filePath":"input_data.md","lastUpdated":null}'),p={name:"input_data.md"},c={class:"jldocstring custom-block",open:""};function u(k,e,g,y,f,E){const t=n("Badge");return d(),l("div",null,[e[4]||(e[4]=s('<h1 id="Input-data" tabindex="-1">Input data <a class="header-anchor" href="#Input-data" aria-label="Permalink to &quot;Input data {#Input-data}&quot;">​</a></h1><p>Several components will allow you to specify your own input data. You can either do this with the <a href="/./SpeedyWeatherDocumentation/dev/api#SpeedyWeather.set!"><code>set!</code></a> method, passing on a <code>Field</code> to a variable, or many components allow for a <code>path</code> and <code>filename</code>. See for example <a href="/./SpeedyWeatherDocumentation/dev/orography#Load-orography-from-file">Load orography from file</a> or the <a href="/./SpeedyWeatherDocumentation/dev/land_sea_mask#Earth&#39;s-land-sea-mask">Earth&#39;s land-sea mask</a> option described in <a href="/./SpeedyWeatherDocumentation/dev/land_sea_mask#The-land-sea-mask">The land-sea mask</a>.</p><p>However, we also use SpeedyWeatherAssets.jl to handle different versions of input data with Julia&#39;s Artifacts system (<code>Artifacts.jl</code> and <code>Pkg.Artifacts</code>) which will automatically download such data on-demand. This helps us to reduce SpeedyWeather&#39;s repository size to a minimum while only downloading data that is actually needed, and only downloading it once. For example, this means that if you only use SpeedyWeather&#39;s <code>BarotropicModel</code> you never have to download orography or a land-sea mask.</p><p>The following explains how we set up <a href="https://github.com/SpeedyWeather/SpeedyWeatherAssets" target="_blank" rel="noreferrer">SpeedyWeatherAssets</a> to work with <code>SpeedyWeather.get_asset</code> or <code>RingGrids.get_asset</code> using different versions of input data or from experimental branches. The primary intended use case is to support different data-driven parameterizations with specific neural network weights that all come in different files that need to be loaded in. The asset handling is part of <code>RingGrids</code>, but exported so that you can use them with <code>SpeedyWeather</code> directly as well.</p><h2 id="SpeedyWeatherAssets" tabindex="-1">SpeedyWeatherAssets <a class="header-anchor" href="#SpeedyWeatherAssets" aria-label="Permalink to &quot;SpeedyWeatherAssets {#SpeedyWeatherAssets}&quot;">​</a></h2><p>The interface to load data from <a href="https://github.com/SpeedyWeather/SpeedyWeatherAssets" target="_blank" rel="noreferrer">SpeedyWeatherAssets</a> is via the <code>get_asset</code> function</p>',6)),a("details",c,[a("summary",null,[e[0]||(e[0]=a("a",{id:"RingGrids.get_asset-input_data",href:"#RingGrids.get_asset-input_data"},[a("span",{class:"jlbinding"},"RingGrids.get_asset")],-1)),e[1]||(e[1]=r()),i(t,{type:"info",class:"jlObjectType jlFunction",text:"Function"})]),e[3]||(e[3]=s(`<div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">get_asset</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    path</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    from_assets,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    name,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ArrayType,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    FileFormat,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    assets_url,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    version,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    fill_value,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    output_grid</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Downloads a file under <code>path</code> from an assets repository (if <code>from_assets=true</code>, otherwise loads locally), adds it to Artifacts.toml in the package directory, and returns the loaded data. Use <code>version = &quot;branch&quot;</code> (a String) to retrieve data from a branch, e.g. <code>version = &quot;main&quot;</code> for the latest on the main branch. <code>name</code> is used to find a variable inside the file, <code>ArrayType</code> to determine how to wrap the loaded data (in most cases into a <code>Field</code> subtype, determining the grid). <code>FileFormat</code> determines how to read the file; load NCDatasets.jl to enable reading NetCDF files. <code>assets_url</code> can be set to a custom URL for alternative asset repositories (e.g. for Terrarium). <code>version</code> is the version tag (a <code>VersionNumber</code>) or branch name (a <code>String</code>) to download from. <code>fill_value</code> is the value used to replace missing data (identified by the file&#39;s <code>_FillValue</code> attribute) in the loaded array; defaults to <code>NaN</code>. <code>output_grid</code> is the grid to interpolate the loaded data to; defaults to <code>nothing</code> (no interpolation). <code>ArrayType</code> must be a <code>AbstractFullField</code> type for interpolation to work.</p>`,2)),i(t,{type:"info",class:"source-link",text:"source"},{default:h(()=>[...e[2]||(e[2]=[a("a",{href:"https://github.com/SpeedyWeather/SpeedyWeather.jl/blob/7ab2de168c53180ca4fb9d87e6d398ce67b13111/RingGrids/src/get_asset.jl#L16",target:"_blank",rel:"noreferrer"},"source",-1)])]),_:1})]),e[5]||(e[5]=s(`<p>with <code>from_assets = true</code> will look into that repository to find the corresponding file under <code>path</code>, note that the base path starts with <code>&quot;data&quot;</code> and then can have any subfolder(s).</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">joinpath</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;data&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;subfolder&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, file)</span></span></code></pre></div><p>with <code>from_assets = false</code> will look for this <code>file</code> locally instead.</p><p>The function <code>get_asset</code> has a keyword argument <code>version</code>, use</p><ul><li><p><code>v&quot;1&quot;</code> (a <code>VersionNumber</code>) for released versions of SpeedyWeatherAssets, we follow semantic versioning.</p></li><li><p><code>&quot;branch&quot;</code> (a <code>String</code>) to use data in a given <code>branch</code> of the SpeedyWeatherAssets repository.</p></li></ul><p>In the latter case the version number (here <code>1</code>) is actually ignored and the</p><p>Then there are options how to read the data from file and interpret it. <code>FileFormat</code> determines the file format, e.g. <code>NCDataset</code> will which attempt to read in a netCDF file. <code>name</code> will read a variable of that name inside the file and <code>ArrayType</code> is the type the data array is converted to or wrapped in, in most cases this would be a <code>FullGaussianField</code> or <code>FullClenshawField</code>. Note that this excludes the <code>on_architecture</code> call that will automatically convert to a <code>GPUArray</code>. To define a new way to read in and interpret an asset extend the following function</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">_get_asset</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(path</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, name</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, ArrayType</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Type{&lt;:YourArraType}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, FileFormat</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Type{&lt;:YourFileFormat}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>for <code>YourArrayType</code> and <code>YourFileFormat</code>.</p><p>When calling <code>get_asset</code> a new asset will be downloaded and stored where other Julia Artifacts are stored, by default this would be <code>.julia/artifacts</code> and then folders of their respective hash. Using assets in SpeedyWeather one does not handle the Artifacts.toml file directly but this will be placed in the root folder (or inside <code>SpeedyWeather/src/input</code> when loading locally with <code>.SpeedyWeather</code>). Deleting this file will redownload all demanded assets but one can also delete individiual ones.</p><h2 id="Managing-new-SpeedyWeatherAssets-versions" tabindex="-1">Managing new SpeedyWeatherAssets versions <a class="header-anchor" href="#Managing-new-SpeedyWeatherAssets-versions" aria-label="Permalink to &quot;Managing new SpeedyWeatherAssets versions {#Managing-new-SpeedyWeatherAssets-versions}&quot;">​</a></h2><p>The current default version of SpeedyWeatherAssets used is</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> RingGrids</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">RingGrids</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">DEFAULT_ASSETS_VERSION</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;">v&quot;1.0.0&quot;</span></span></code></pre></div><p>For new released versions one should (manually) add that version to</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">RingGrids</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">AVAILABLE_ASSETS_VERSIONS</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;">(v&quot;1.0.0&quot;,)</span></span></code></pre></div><p>only those versions can be used in <code>version = v&quot;1.1&quot;</code> for example. Branches of SpeedyWeatherAssets, e.g. <code>version = &quot;main&quot;</code> do not have to be added to the available versions.</p>`,18))])}const v=o(p,[["render",u]]);export{b as __pageData,v as default};
